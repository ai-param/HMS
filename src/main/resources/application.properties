spring.application.name=hms
# Use PORT env variable if available (Render/Heroku), otherwise default to 8080
server.port=${PORT:8080}
server.servlet.session.timeout=1m

# Database - NeonDB (PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-plain-mud-ah3flp2s-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_EZkiSuB2VwD6
spring.datasource.driverClassName=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
